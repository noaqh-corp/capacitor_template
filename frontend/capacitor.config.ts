import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'drink',
  webDir: 'dist',
  server:{
    hostname: `aa.wecqhvbkol.a.pinggy.link`,
    androidScheme: "https",
  }
};

export default config;
